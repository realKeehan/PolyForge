# Go / Wails
bin/
dist/
build/
!frontend/dist/
frontend/dist/*
!frontend/dist/README.md
frontend/node_modules/
frontend/wailsjs/
*.exe
*.dll
*.log
.DS_Store

# MkDocs build output
site/

# The website now lives in its own (private) repo: realKeehan/polyforge-web.
# It is still checked out here at website/ so scripts/package-website.ps1 keeps
# working unchanged — but it is a separate git repo, so this one ignores it.
# Clone it back with:  git clone git@github.com:realKeehan/polyforge-web.git website
website/

launcher-trees-*/

# Superseded static site that used to live at the repo root, kept locally for
# reference only. See archive/legacy-site/README.md.
archive/

# Local reference/scratch material (not a project artifact)
TemporaryDetectRef/
