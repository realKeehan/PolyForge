# Documentation build dependencies (MkDocs + Material theme).
# Used by the docs GitHub Actions workflow and for local previews:
#   pip install -r requirements-docs.txt
#   mkdocs serve
mkdocs-material>=9.5,<10
