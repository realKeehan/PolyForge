# Launcher install matrix (internal roadmap)

Scaffolding for updating and implementing the per-launcher install paths and
the pack build scripts. Sources of truth, in order:

1. **Original KUMI PowerShell installer** — `TemporaryDetectRef/OriginalKumiScripts/KUMI.ps1`
   (v5.5.1). The Go install modules under `internal/kumi/install/` are direct
   ports of its menu cases.
2. **Reference machine dumps** — `TemporaryDetectRef/MachineTest_01/` and
   `TemporaryDetectRef/machine test 2/launcher-trees-2026-07-11_1415/`
   (produced by `scripts/dump-launcher-trees.ps1`, v4). Every layout claim
   below marked *(verified)* was read from those trees.
3. **Current code** — detection `internal/kumi/detect.go`, layout planning +
   generation registry `internal/kumi/packformat.go` (`launcherTargets`),
   writers `internal/kumi/gen_launchers.go`, legacy modules
   `internal/kumi/install/*.go`, hosted flow `internal/kumi/hostedpack.go`.

## The two install paths

- **Legacy path** (ported from KUMI.ps1): per-launcher buttons →
  `Service.installX()` (`launchers.go`) → `install.X(...)` → find root →
  create `<root>\<subdir>\TurtelSMP5` → download a fixed ZIP.
  **The legacy ZIPs are dead**: they are Discord CDN attachment links
  (`service.go` consts) that now carry expiring signatures — the Modrinth URL's
  embedded `ex=` stamp is long past. Most launchers never had a ZIP at all and
  fall through to "No ZIP URL configured".
- **Modern path** (hosted `.polypack`): download from the pack registry →
  `extractAndVerifyPack` → `PlanInstallDirs` → extract overrides into the game
  dir → `GenerateLauncherFiles` writers → per-file integrity verification.
  Launcher-agnostic and already reference-verified for the layouts below.

**Migration goal:** every legacy button becomes a pack-driven install — the
per-launcher module keeps only root resolution (detection candidates), and
layout + config generation come from `launcherTargets`. The staging scaffold
for this is `internal/kumi/install/packflow.go` (`PackInstance`).

## Install modes

Implemented in `internal/kumi/installmodes.go` and applied by
`extractAndVerifyPack` between layout planning and extraction. The UI
requests a mode via `payload.Extra["mode"]` (both the hosted and local pack
paths accept it); unknown/empty values resolve as `auto`, so an outdated
frontend can never break installs. `PackInstance` carries the mode through
to its stager for the migrated legacy buttons.

| Mode | Existing game dir treatment | User data |
| --- | --- | --- |
| `auto` | `update` when the dir carries `.polyforge-pack.json`, else `install` | — |
| `install` | Legacy lay-down: shipped files overwrite same-path files, everything else untouched | untouched |
| `update` | Sweeps files the OLD pack shipped that the NEW pack dropped (candidates come only from the old manifest's overrides list), then extracts | untouchable by construction |
| `reinstall` | Repair: same sweep + full re-extract restores missing/modified files (`VerifyInstalledPack` reports which) | untouchable by construction |
| `clean` | Quarantines the entire game dir into `.polyforge-quarantine\<timestamp>\` first — generalizes KUMI.ps1's "not-turtel" move; nothing is deleted | moved to quarantine, recoverable |

Notes: `update`/`reinstall` on a dir without a record fall back to `install`;
packs that predate per-file manifests cannot be swept (logged as such);
`PlanPackUpdate` (same file) powers "update available" previews via
`ComparePackMods`. Clean covers the game dir only — launcher instance
metadata is regenerated by the config writers.

## Matrix

Status legend: ✅ ready (layout verified + generator exists) · 🔧 needs-fix
(known defect) · ❓ needs-verify (open question, next machine test) ·
🅿️ parked (deliberate, see below) · ➖ n/a.

| Launcher | KUMI.ps1 origin | Data root *(verified)* | Instance path under root | Config writer (`gen_launchers.go`) | Legacy module (`install/`) | Status / next step |
| --- | --- | --- | --- | --- | --- | --- |
| vanilla | menu 1: KUMIProfiles zip + Quilt zip into `versions\` + `launcher_profiles.json` upsert | `%APPDATA%\.minecraft` | profile `gameDir` points anywhere | `genVanillaProfile` (fetches loader profile JSON) | `vanilla.go` (full port incl. profile upsert) | ✅ migrate zips → pack flow |
| multimc | menu 2: exe probe, 3 roots | portable — shortcut/scan discovery | `instances\<name>\.minecraft` | `genMMCInstance(false)` | `multimc.go` | ✅ |
| polymc | menu 11 | `%APPDATA%\PolyMC` | `instances\<name>\.minecraft` | `genMMCInstance(false)` | `polymc.go` | ✅ |
| prismlauncher | menu 7 | `%APPDATA%\PrismLauncher` | `instances\<name>\minecraft` | `genMMCInstance(true)` | `prismlauncher.go` | ✅ |
| shatteredprism | — (post-KUMI) | `%APPDATA%\ShatteredPrism` | `instances\<name>\minecraft` | `genMMCInstance(true)` | `planned.go` | ✅ |
| elyprism | — | `%APPDATA%\ElyPrismLauncher` | `instances\<name>\minecraft` | `genMMCInstance(true)` | `planned.go` | ✅ |
| fjord | — | `%APPDATA%\FjordLauncher` | `instances\<name>\minecraft` | `genMMCInstance(true)` | `planned.go` | ✅ |
| freesm | — | `%APPDATA%\FreesmLauncher` | `instances\<name>\minecraft` | `genMMCInstance(true)` | `planned.go` | ✅ |
| modrinth | menu 4 (used old `com.modrinth.theseus` path) | `%APPDATA%\ModrinthApp`, overridable via `settings.custom_dir` in app.db — **may be set to the default path** *(verified)* | `profiles\<name>` (root = game dir) | `genModrinthProfile` (sqlite row, templates from an existing row) | `modrinth.go` | ✅ needs ≥1 existing profile row to template |
| curseforge | menu 3 | `%USERPROFILE%\curseforge\minecraft` | `Instances\<name>` (root = game dir) | `genCurseForgeInstance` | `curseforge.go` | 🔧 generator does not write the `.curseclient` marker (bare instance GUID file; every real instance has one *(verified)*) |
| atlauncher | menu 6 | portable; exe dir = data dir (stray-copy scan fix 2026-07-11) | `instances\<name>` (root = game dir) | none — instance.json embeds full Mojang+loader manifests (~70 KB) | `atlauncher.go` | ➖ semi-manual: extract overrides, user adds pack in ATLauncher |
| gdlauncher | menu 5 (probed legacy `gdlauncher_next`/`gdlauncher`) | `%APPDATA%\gdlauncher_carbon` *(verified)* | **`data\instances\<name>`**, game dir `...\instance\` *(verified)* | `genGDLauncherInstance` | `gdlauncher.go` | 🔧 `instancesDirName("gdlauncher")` returns `instances` but Carbon nests them under `data\instances` — `PlanInstallDirs` targets the wrong folder when handed the detected root |
| technic | menu 10 | `%APPDATA%\.technic` | `modpacks\<slug>` | none | `technic.go` | 🅿️ parked + ❓ `InstanceSubdir: "bin"` is suspect — mods/config sit at the modpack root, `bin\` holds `modpack.jar` |
| dawn | menu 9 (as Feather) | `%APPDATA%\.dawn` *(verified)* | `profiles\<id>` (kebab-case id), game dir `.minecraft\` *(verified)* | `genDawnProfile` | `dawn.go` | ✅ edge: installed-but-never-run has no `.dawn` and no exe fallback wired |
| bakaxl | menu 8 | `%APPDATA%\BakaXL` | unknown — `CoreDirectory.json` schema still uncaptured (scrubbed from test 2; dump v4 will capture it) | none | `bakaxl.go` (assumes `instances\`, unverified) | 🅿️ parked (language barrier) |
| sklauncher | — | **uses vanilla `.minecraft` directly** *(verified)*: registers a `launcher_profiles.json` profile with `gameDir` `.minecraft\instances\<name>` | n/a | vanilla writer is the right vehicle | `planned.go` (assumes own `instances\` — wrong) | 🔧 rewire to the vanilla flow with an `instances\<name>` game dir |
| qwertz | — | `%APPDATA%\QWERTZ-Launcher` (exe lives inside data dir) *(verified)* | `profiles\<name>` (root = game dir) *(verified)* | `genQwertzProfile` (registry upsert, added 2026-07-11) | `planned.go` | ✅ ❓ only `FABRIC` loader enum value is reference-verified |
| hmcl | — | `%APPDATA%\.hmcl` / `~\.hmcl` (missing on ref machine) | unknown | none | `planned.go` | 🅿️ parked (language barrier) |
| ultimmc | — | `%APPDATA%\UltimMC` (missing on ref machine) | `instances\<name>\.minecraft` (assumed, MultiMC fork) | `genMMCInstance(false)` | `planned.go` | 🅿️ parked (no obtainable download) |
| polymerium | — | `%LOCALAPPDATA%\Trident` (settings in `%APPDATA%\Polymerium`) *(verified)* | `instances\<id>` | `genPolymeriumProfile` | `planned.go` | ❓ live game dir appears to be `instances\<id>\build\` after deploy *(verified tree)* — overrides at the instance root may be invisible; likely `InstanceSubdir: "build"` |
| xmcl | — | `%USERPROFILE%\.minecraftx` (config in `%APPDATA%\xmcl`) *(verified)* | `instances\<name>` (root = game dir) *(verified)* | `genXMCLInstance`; registry needs no upsert — XMCL rescans on boot | `planned.go` | ✅ |
| custom | menu 12 (`bypass.turtel` / `not-turtel` quarantine) | user-chosen mods dir | n/a | n/a | `custom.go` (full port) | ✅ migrate zip → pack flow |
| manual | menu 13 (zip → cwd) | user-chosen dir | n/a | n/a | `manual.go` | ✅ migrate zip → pack flow |

## Action items (ordered)

1. **GDLauncher `data\instances`** — teach `instancesDirName`/`PlanInstallDirs`
   that Carbon's instances live one level down (`data\instances`). Mind the
   "user chose the instances folder itself" comparison, which today only
   matches a single path segment.
2. **CurseForge `.curseclient`** — `genCurseForgeInstance` should write the
   marker file (contents = the instance GUID it already generates).
3. **SKLauncher → vanilla flow** — drop the fake `instances\` module; route
   installs to the vanilla writer with game dir
   `%APPDATA%\.minecraft\instances\<name>`.
4. **Polymerium `build\`** — verify on the reference machine whether files
   placed at the instance root survive/deploy; then set `InstanceSubdir`.
5. **Legacy button migration** — swap `InstanceWithOptionalZip` modules to the
   `PackInstance` scaffold (`install/packflow.go`) launcher by launcher, wiring
   plan+stage closures from `kumi` (see the scaffold's doc comment). Delete the
   dead Discord CDN consts from `service.go` when the last button is migrated.
6. **QWERTZ loader enum** — confirm the non-Fabric spellings in QWERTZ's
   `profiles.json` on the next machine test (only `FABRIC` observed).
7. **Docs** — refresh the public tiers in `docs/launchers.md` once 1–5 land.

## Parked (deliberate, 2026-07-11)

- **Technic** — pending the custom-pack question: Technic may be a pack
  *provider* only (packs install from its site); `installedPacks` on the
  reference machine was empty, so there is no custom-pack row to mirror.
- **BakaXL / HMCL** — language barrier; BakaXL's `CoreDirectory.json` is also
  still uncaptured (dump v4 will pick it up next run).
- **UltimMC** — no obtainable download.

## Build scripts

- `scripts/package-modpack.ps1` builds the `.polypack` (manifest + launchers +
  overrides). It emits a `launchers.json` entry for **every** launcher above,
  plus a per-launcher `status` field (`ready` / `verify` / `parked`) mirroring
  this matrix so the installer and website can gate options without a schema
  change (`LaunchersFile` decodes entries as open maps).
- `scripts/dump-launcher-trees.ps1` (v4) is the evidence pipeline: rerun it on
  the reference machine after any launcher update and re-verify this matrix
  against the `FILES\` copies before flipping a status to ✅.
- `scripts/wails-build.ps1` / `docs/building.md` cover the app binary itself
  and are unaffected by per-launcher work.
