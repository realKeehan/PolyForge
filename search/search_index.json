{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"PolyForge","text":"<p>Keehan's Universal Modpack Installer (KUMI) rebuilt as a Wails desktop app. The backend mirrors the original PowerShell automation in Go, and the frontend is a multi-step wizard built with Pug, SCSS, and TypeScript.</p> <p>Supported on Windows, Linux, and macOS.</p> <p>Download View on GitHub</p>"},{"location":"#what-it-does","title":"What it does","text":"<p>PolyForge installs modpacks across many Minecraft launchers from a single workflow. It discovers installed launchers, guides you through picking an action, modpack, and launcher, then streams structured logs while it performs the install.</p> <p>One pack format (<code>.polypack</code>) installs everywhere: packs are launcher-agnostic, and the installer generates each launcher's real profile/instance files at install time.</p>"},{"location":"#documentation","title":"Documentation","text":"<ul> <li> <p>:material-hammer-wrench: Building from source</p> <p>Prerequisites, dev mode, and the release build script options.</p> </li> <li> <p>:material-rocket-launch: Supported launchers</p> <p>Which launchers work today and what's in progress.</p> </li> <li> <p>:material-package-variant-closed: Modpack format</p> <p>The <code>.polypack</code> container, manifest schema, and update flow.</p> </li> </ul>"},{"location":"#launcher-detection","title":"Launcher detection","text":"<p>PolyForge uses a multi-strategy resolver that discovers installed launchers through a cache of validated paths, common install locations, the Windows registry, the Store AppsFolder, running processes, Start Menu shortcuts, and a last-resort targeted filesystem scan. Users can also browse to a launcher manually, which is cached with the highest priority.</p>"},{"location":"#project-links","title":"Project links","text":"<ul> <li>Website: https://polyforge.dev</li> <li>Source: https://github.com/realKeehan/PolyForge</li> </ul>"},{"location":"building/","title":"Building from source","text":""},{"location":"building/#prerequisites","title":"Prerequisites","text":"Tool Minimum version Notes Go 1.21+ Backend compilation Node.js 18+ Frontend build toolchain npm (bundled with Node) Dependency management Wails CLI v2 Desktop shell + bindings UPX (optional) Binary compression (<code>-UPX</code> flag) garble (optional) Bound-method obfuscation (<code>-Obfuscated</code> flag)"},{"location":"building/#getting-started","title":"Getting started","text":"<ol> <li> <p>Install Go 1.21+, Node 18+, and the Wails CLI.</p> </li> <li> <p>Install and build the frontend bundle:</p> <pre><code>cd frontend\nnpm ci\nnpm run build\n</code></pre> </li> <li> <p>From the project root, start the app in development mode:</p> WindowsmacOS / Linux <pre><code>pwsh scripts/wails-dev.ps1\n</code></pre> <p>Handles npm install, Go toolchain normalisation, and stale binding cleanup.</p> <pre><code>wails dev\n</code></pre> </li> <li> <p>To produce a release build on Windows:</p> <pre><code>pwsh scripts/wails-build.ps1\n</code></pre> </li> </ol> <p>Dev menu</p> <p><code>dev-menu.bat</code> wraps the common tasks (dev mode, release build with interactive flag selection, website localhost, packaging, version bump).</p>"},{"location":"building/#build-script-options","title":"Build script options","text":"Flag Description <code>-UPX</code> Compress the output binary with UPX. Requires <code>upx</code> on PATH. <code>-Obfuscated</code> Obfuscate bound Wails methods via garble (<code>wails build -obfuscated</code>). Requires <code>go install mvdan.cc/garble@latest</code>. <code>-SkipFrontend</code> Skip <code>npm ci</code> and <code>npm run build</code> if the frontend is already built. <p>Any other flags are forwarded straight to <code>wails build</code> (e.g. <code>-nsis</code>, <code>-clean</code>, <code>-trimpath</code>, <code>-webview2 embed</code>, <code>-debug</code>).</p>"},{"location":"building/#examples","title":"Examples","text":"<pre><code># Standard build\npwsh scripts/wails-build.ps1\n\n# UPX + obfuscation\npwsh scripts/wails-build.ps1 -UPX -Obfuscated\n\n# NSIS installer output\npwsh scripts/wails-build.ps1 -nsis\n\n# Skip the frontend rebuild (e.g. CI built it in a prior step)\npwsh scripts/wails-build.ps1 -SkipFrontend\n</code></pre> <p>Windows PowerShell 5.1</p> <p>The build scripts are compatible with Windows PowerShell 5.1 (the default <code>powershell.exe</code>) as well as PowerShell 7 (<code>pwsh</code>). If you invoke them through the dev menu without <code>pwsh</code> installed, they fall back to 5.1.</p>"},{"location":"building/#troubleshooting","title":"Troubleshooting","text":""},{"location":"building/#assets_embedgo-pattern-frontenddist-no-matching-files-found","title":"<code>assets_embed.go: pattern frontend/dist: no matching files found</code>","text":"<p>The Go compiler needs the frontend bundle to exist at <code>frontend/dist/</code> before building. The build scripts do this automatically. If building manually or in CI, run the frontend build first:</p> <pre><code>cd frontend &amp;&amp; npm ci &amp;&amp; npm run build &amp;&amp; cd ..\ngo build -v ./...\n</code></pre>"},{"location":"building/#windows-binding-errors","title":"Windows binding errors","text":"<p>If Wails reports \"This version of %1 is not compatible with the version of Windows you're running\" when generating bindings, the cached helper at <code>%TEMP%\\wailsbindings.exe</code> is usually stale (compiled for the wrong architecture). The PowerShell scripts delete it automatically before each build; remove it manually and retry if the error persists.</p>"},{"location":"building/#distribution-formats","title":"Distribution formats","text":"<p>PolyForge targets these formats across platforms (see the downloads page):</p> WindowsmacOSLinux <ul> <li><code>.exe</code> (standard + NSIS installer)</li> <li><code>.exe</code> (portable)</li> <li><code>.exe</code> (UPX-compressed)</li> <li><code>.zip</code> (portable archive)</li> </ul> <ul> <li><code>.app</code> bundle</li> <li><code>.dmg</code> (disk image)</li> <li><code>.zip</code> (portable)</li> </ul> <ul> <li>AppImage</li> <li><code>.deb</code> / <code>.rpm</code></li> <li><code>.tar.gz</code></li> <li>Flatpak / Snap / AUR (community)</li> </ul> <p>Each release publishes a <code>SHA256SUMS.txt</code> alongside the builds so downloads can be verified.</p>"},{"location":"launcher-install-matrix/","title":"Launcher install matrix (internal roadmap)","text":"<p>Scaffolding for updating and implementing the per-launcher install paths and the pack build scripts. Sources of truth, in order:</p> <ol> <li>Original KUMI PowerShell installer \u2014 <code>TemporaryDetectRef/OriginalKumiScripts/KUMI.ps1</code>    (v5.5.1). The Go install modules under <code>internal/kumi/install/</code> are direct    ports of its menu cases.</li> <li>Reference machine dumps \u2014 <code>TemporaryDetectRef/MachineTest_01/</code> and    <code>TemporaryDetectRef/machine test 2/launcher-trees-2026-07-11_1415/</code>    (produced by <code>scripts/dump-launcher-trees.ps1</code>, v4). Every layout claim    below marked (verified) was read from those trees.</li> <li>Current code \u2014 detection <code>internal/kumi/detect.go</code>, layout planning +    generation registry <code>internal/kumi/packformat.go</code> (<code>launcherTargets</code>),    writers <code>internal/kumi/gen_launchers.go</code>, legacy modules    <code>internal/kumi/install/*.go</code>, hosted flow <code>internal/kumi/hostedpack.go</code>.</li> </ol>"},{"location":"launcher-install-matrix/#the-two-install-paths","title":"The two install paths","text":"<ul> <li>Legacy path (ported from KUMI.ps1): per-launcher buttons \u2192   <code>Service.installX()</code> (<code>launchers.go</code>) \u2192 <code>install.X(...)</code> \u2192 find root \u2192   create <code>&lt;root&gt;\\&lt;subdir&gt;\\TurtelSMP5</code> \u2192 download a fixed ZIP.   The legacy ZIPs are dead: they are Discord CDN attachment links   (<code>service.go</code> consts) that now carry expiring signatures \u2014 the Modrinth URL's   embedded <code>ex=</code> stamp is long past. Most launchers never had a ZIP at all and   fall through to \"No ZIP URL configured\".</li> <li>Modern path (hosted <code>.polypack</code>): download from the pack registry \u2192   <code>extractAndVerifyPack</code> \u2192 <code>PlanInstallDirs</code> \u2192 extract overrides into the game   dir \u2192 <code>GenerateLauncherFiles</code> writers \u2192 per-file integrity verification.   Launcher-agnostic and already reference-verified for the layouts below.</li> </ul> <p>Migration goal: every legacy button becomes a pack-driven install \u2014 the per-launcher module keeps only root resolution (detection candidates), and layout + config generation come from <code>launcherTargets</code>. The staging scaffold for this is <code>internal/kumi/install/packflow.go</code> (<code>PackInstance</code>).</p>"},{"location":"launcher-install-matrix/#install-modes","title":"Install modes","text":"<p>Implemented in <code>internal/kumi/installmodes.go</code> and applied by <code>extractAndVerifyPack</code> between layout planning and extraction. The UI requests a mode via <code>payload.Extra[\"mode\"]</code> (both the hosted and local pack paths accept it); unknown/empty values resolve as <code>auto</code>, so an outdated frontend can never break installs. <code>PackInstance</code> carries the mode through to its stager for the migrated legacy buttons.</p> Mode Existing game dir treatment User data <code>auto</code> <code>update</code> when the dir carries <code>.polyforge-pack.json</code>, else <code>install</code> \u2014 <code>install</code> Legacy lay-down: shipped files overwrite same-path files, everything else untouched untouched <code>update</code> Sweeps files the OLD pack shipped that the NEW pack dropped (candidates come only from the old manifest's overrides list), then extracts untouchable by construction <code>reinstall</code> Repair: same sweep + full re-extract restores missing/modified files (<code>VerifyInstalledPack</code> reports which) untouchable by construction <code>clean</code> Quarantines the entire game dir into <code>.polyforge-quarantine\\&lt;timestamp&gt;\\</code> first \u2014 generalizes KUMI.ps1's \"not-turtel\" move; nothing is deleted moved to quarantine, recoverable <p>Notes: <code>update</code>/<code>reinstall</code> on a dir without a record fall back to <code>install</code>; packs that predate per-file manifests cannot be swept (logged as such); <code>PlanPackUpdate</code> (same file) powers \"update available\" previews via <code>ComparePackMods</code>. Clean covers the game dir only \u2014 launcher instance metadata is regenerated by the config writers.</p>"},{"location":"launcher-install-matrix/#matrix","title":"Matrix","text":"<p>Status legend: \u2705 ready (layout verified + generator exists) \u00b7 \ud83d\udd27 needs-fix (known defect) \u00b7 \u2753 needs-verify (open question, next machine test) \u00b7 \ud83c\udd7f\ufe0f parked (deliberate, see below) \u00b7 \u2796 n/a.</p> Launcher KUMI.ps1 origin Data root (verified) Instance path under root Config writer (<code>gen_launchers.go</code>) Legacy module (<code>install/</code>) Status / next step vanilla menu 1: KUMIProfiles zip + Quilt zip into <code>versions\\</code> + <code>launcher_profiles.json</code> upsert <code>%APPDATA%\\.minecraft</code> profile <code>gameDir</code> points anywhere <code>genVanillaProfile</code> (fetches loader profile JSON) <code>vanilla.go</code> (full port incl. profile upsert) \u2705 migrate zips \u2192 pack flow multimc menu 2: exe probe, 3 roots portable \u2014 shortcut/scan discovery <code>instances\\&lt;name&gt;\\.minecraft</code> <code>genMMCInstance(false)</code> <code>multimc.go</code> \u2705 polymc menu 11 <code>%APPDATA%\\PolyMC</code> <code>instances\\&lt;name&gt;\\.minecraft</code> <code>genMMCInstance(false)</code> <code>polymc.go</code> \u2705 prismlauncher menu 7 <code>%APPDATA%\\PrismLauncher</code> <code>instances\\&lt;name&gt;\\minecraft</code> <code>genMMCInstance(true)</code> <code>prismlauncher.go</code> \u2705 shatteredprism \u2014 (post-KUMI) <code>%APPDATA%\\ShatteredPrism</code> <code>instances\\&lt;name&gt;\\minecraft</code> <code>genMMCInstance(true)</code> <code>planned.go</code> \u2705 elyprism \u2014 <code>%APPDATA%\\ElyPrismLauncher</code> <code>instances\\&lt;name&gt;\\minecraft</code> <code>genMMCInstance(true)</code> <code>planned.go</code> \u2705 fjord \u2014 <code>%APPDATA%\\FjordLauncher</code> <code>instances\\&lt;name&gt;\\minecraft</code> <code>genMMCInstance(true)</code> <code>planned.go</code> \u2705 freesm \u2014 <code>%APPDATA%\\FreesmLauncher</code> <code>instances\\&lt;name&gt;\\minecraft</code> <code>genMMCInstance(true)</code> <code>planned.go</code> \u2705 modrinth menu 4 (used old <code>com.modrinth.theseus</code> path) <code>%APPDATA%\\ModrinthApp</code>, overridable via <code>settings.custom_dir</code> in app.db \u2014 may be set to the default path (verified) <code>profiles\\&lt;name&gt;</code> (root = game dir) <code>genModrinthProfile</code> (sqlite row, templates from an existing row) <code>modrinth.go</code> \u2705 needs \u22651 existing profile row to template curseforge menu 3 <code>%USERPROFILE%\\curseforge\\minecraft</code> <code>Instances\\&lt;name&gt;</code> (root = game dir) <code>genCurseForgeInstance</code> <code>curseforge.go</code> \ud83d\udd27 generator does not write the <code>.curseclient</code> marker (bare instance GUID file; every real instance has one (verified)) atlauncher menu 6 portable; exe dir = data dir (stray-copy scan fix 2026-07-11) <code>instances\\&lt;name&gt;</code> (root = game dir) none \u2014 instance.json embeds full Mojang+loader manifests (~70 KB) <code>atlauncher.go</code> \u2796 semi-manual: extract overrides, user adds pack in ATLauncher gdlauncher menu 5 (probed legacy <code>gdlauncher_next</code>/<code>gdlauncher</code>) <code>%APPDATA%\\gdlauncher_carbon</code> (verified) <code>data\\instances\\&lt;name&gt;</code>, game dir <code>...\\instance\\</code> (verified) <code>genGDLauncherInstance</code> <code>gdlauncher.go</code> \ud83d\udd27 <code>instancesDirName(\"gdlauncher\")</code> returns <code>instances</code> but Carbon nests them under <code>data\\instances</code> \u2014 <code>PlanInstallDirs</code> targets the wrong folder when handed the detected root technic menu 10 <code>%APPDATA%\\.technic</code> <code>modpacks\\&lt;slug&gt;</code> none <code>technic.go</code> \ud83c\udd7f\ufe0f parked + \u2753 <code>InstanceSubdir: \"bin\"</code> is suspect \u2014 mods/config sit at the modpack root, <code>bin\\</code> holds <code>modpack.jar</code> dawn menu 9 (as Feather) <code>%APPDATA%\\.dawn</code> (verified) <code>profiles\\&lt;id&gt;</code> (kebab-case id), game dir <code>.minecraft\\</code> (verified) <code>genDawnProfile</code> <code>dawn.go</code> \u2705 edge: installed-but-never-run has no <code>.dawn</code> and no exe fallback wired bakaxl menu 8 <code>%APPDATA%\\BakaXL</code> unknown \u2014 <code>CoreDirectory.json</code> schema still uncaptured (scrubbed from test 2; dump v4 will capture it) none <code>bakaxl.go</code> (assumes <code>instances\\</code>, unverified) \ud83c\udd7f\ufe0f parked (language barrier) sklauncher \u2014 uses vanilla <code>.minecraft</code> directly (verified): registers a <code>launcher_profiles.json</code> profile with <code>gameDir</code> <code>.minecraft\\instances\\&lt;name&gt;</code> n/a vanilla writer is the right vehicle <code>planned.go</code> (assumes own <code>instances\\</code> \u2014 wrong) \ud83d\udd27 rewire to the vanilla flow with an <code>instances\\&lt;name&gt;</code> game dir qwertz \u2014 <code>%APPDATA%\\QWERTZ-Launcher</code> (exe lives inside data dir) (verified) <code>profiles\\&lt;name&gt;</code> (root = game dir) (verified) <code>genQwertzProfile</code> (registry upsert, added 2026-07-11) <code>planned.go</code> \u2705 \u2753 only <code>FABRIC</code> loader enum value is reference-verified hmcl \u2014 <code>%APPDATA%\\.hmcl</code> / <code>~\\.hmcl</code> (missing on ref machine) unknown none <code>planned.go</code> \ud83c\udd7f\ufe0f parked (language barrier) ultimmc \u2014 <code>%APPDATA%\\UltimMC</code> (missing on ref machine) <code>instances\\&lt;name&gt;\\.minecraft</code> (assumed, MultiMC fork) <code>genMMCInstance(false)</code> <code>planned.go</code> \ud83c\udd7f\ufe0f parked (no obtainable download) polymerium \u2014 <code>%LOCALAPPDATA%\\Trident</code> (settings in <code>%APPDATA%\\Polymerium</code>) (verified) <code>instances\\&lt;id&gt;</code> <code>genPolymeriumProfile</code> <code>planned.go</code> \u2753 live game dir appears to be <code>instances\\&lt;id&gt;\\build\\</code> after deploy (verified tree) \u2014 overrides at the instance root may be invisible; likely <code>InstanceSubdir: \"build\"</code> xmcl \u2014 <code>%USERPROFILE%\\.minecraftx</code> (config in <code>%APPDATA%\\xmcl</code>) (verified) <code>instances\\&lt;name&gt;</code> (root = game dir) (verified) <code>genXMCLInstance</code>; registry needs no upsert \u2014 XMCL rescans on boot <code>planned.go</code> \u2705 custom menu 12 (<code>bypass.turtel</code> / <code>not-turtel</code> quarantine) user-chosen mods dir n/a n/a <code>custom.go</code> (full port) \u2705 migrate zip \u2192 pack flow manual menu 13 (zip \u2192 cwd) user-chosen dir n/a n/a <code>manual.go</code> \u2705 migrate zip \u2192 pack flow"},{"location":"launcher-install-matrix/#action-items-ordered","title":"Action items (ordered)","text":"<ol> <li>GDLauncher <code>data\\instances</code> \u2014 teach <code>instancesDirName</code>/<code>PlanInstallDirs</code>    that Carbon's instances live one level down (<code>data\\instances</code>). Mind the    \"user chose the instances folder itself\" comparison, which today only    matches a single path segment.</li> <li>CurseForge <code>.curseclient</code> \u2014 <code>genCurseForgeInstance</code> should write the    marker file (contents = the instance GUID it already generates).</li> <li>SKLauncher \u2192 vanilla flow \u2014 drop the fake <code>instances\\</code> module; route    installs to the vanilla writer with game dir    <code>%APPDATA%\\.minecraft\\instances\\&lt;name&gt;</code>.</li> <li>Polymerium <code>build\\</code> \u2014 verify on the reference machine whether files    placed at the instance root survive/deploy; then set <code>InstanceSubdir</code>.</li> <li>Legacy button migration \u2014 swap <code>InstanceWithOptionalZip</code> modules to the    <code>PackInstance</code> scaffold (<code>install/packflow.go</code>) launcher by launcher, wiring    plan+stage closures from <code>kumi</code> (see the scaffold's doc comment). Delete the    dead Discord CDN consts from <code>service.go</code> when the last button is migrated.</li> <li>QWERTZ loader enum \u2014 confirm the non-Fabric spellings in QWERTZ's    <code>profiles.json</code> on the next machine test (only <code>FABRIC</code> observed).</li> <li>Docs \u2014 refresh the public tiers in <code>docs/launchers.md</code> once 1\u20135 land.</li> </ol>"},{"location":"launcher-install-matrix/#parked-deliberate-2026-07-11","title":"Parked (deliberate, 2026-07-11)","text":"<ul> <li>Technic \u2014 pending the custom-pack question: Technic may be a pack   provider only (packs install from its site); <code>installedPacks</code> on the   reference machine was empty, so there is no custom-pack row to mirror.</li> <li>BakaXL / HMCL \u2014 language barrier; BakaXL's <code>CoreDirectory.json</code> is also   still uncaptured (dump v4 will pick it up next run).</li> <li>UltimMC \u2014 no obtainable download.</li> </ul>"},{"location":"launcher-install-matrix/#build-scripts","title":"Build scripts","text":"<ul> <li><code>scripts/package-modpack.ps1</code> builds the <code>.polypack</code> (manifest + launchers +   overrides). It emits a <code>launchers.json</code> entry for every launcher above,   plus a per-launcher <code>status</code> field (<code>ready</code> / <code>verify</code> / <code>parked</code>) mirroring   this matrix so the installer and website can gate options without a schema   change (<code>LaunchersFile</code> decodes entries as open maps).</li> <li><code>scripts/dump-launcher-trees.ps1</code> (v4) is the evidence pipeline: rerun it on   the reference machine after any launcher update and re-verify this matrix   against the <code>FILES\\</code> copies before flipping a status to \u2705.</li> <li><code>scripts/wails-build.ps1</code> / <code>docs/building.md</code> cover the app binary itself   and are unaffected by per-launcher work.</li> </ul>"},{"location":"launchers/","title":"Supported launchers","text":"<p>PolyForge aims to install the same <code>.polypack</code> across every major Minecraft launcher. Support is rolling out in stages.</p> Status Launchers Supported Vanilla Launcher, MultiMC, CurseForge, Modrinth (Theseus), Custom Path, Manual Install In progress Prism Launcher, ATLauncher, GDLauncher, Technic, PolyMC, Dawn (formerly Feather), BakaXL Planned Polymerium, X Minecraft Launcher, SK Launcher, Freesm Launcher, PineconeMC (formerly ElyPrism), ShatteredPrism, QWERTZ, Fjord Launcher, HMCL, UltimMC"},{"location":"launchers/#how-one-pack-targets-every-launcher","title":"How one pack targets every launcher","text":"<p>A <code>.polypack</code> is launcher-agnostic. It carries a <code>launchers.json</code> with info fields for every supported launcher, and the installer generates each launcher's real files (profiles, instance configs) from those fields plus the pack manifest at install time. The pack never ships launcher-specific files.</p> <p>See the modpack format for the container layout and the per-launcher generation plan.</p>"},{"location":"launchers/#launcher-detection","title":"Launcher detection","text":"<p>Installed launchers are discovered through a multi-strategy resolver:</p> <ol> <li>Cache \u2014 previously validated paths (JSON persistence).</li> <li>Known paths \u2014 common install locations per launcher.</li> <li>Registry \u2014 Windows uninstall keys (<code>InstallLocation</code>, <code>DisplayIcon</code>).</li> <li>Shell AppsFolder \u2014 UWP/Store apps via PowerShell.</li> <li>Running processes \u2014 launchers that are currently open.</li> <li>Start Menu shortcuts \u2014 resolving <code>.lnk</code> targets.</li> <li>Targeted scan \u2014 a depth-limited concurrent filesystem scan (last resort).</li> </ol> <p>Users can also browse to a launcher path manually; manual selections are cached with the highest priority.</p>"},{"location":"modpack-format/","title":"PolyForge modpack format (.polypack)","text":"<p>Status: implemented. Pack layout, packager metadata resolution, and per-launcher instance generation are in place; the per-launcher schemas were captured from real installs on the test machine (<code>TemporaryDetectRef/MachineTest_01</code>, <code>scripts/dump-launcher-trees.ps1</code>). Remaining gaps are listed under \"Open items\" at the bottom.</p>"},{"location":"modpack-format/#the-polypack-container","title":"The .polypack container","text":"<p>A pack ships as <code>&lt;id&gt;-&lt;version&gt;.polypack</code> \u2014 PolyForge's branded container. It is a standard ZIP archive wrapped by a simple, reversible transform so the file isn't a plain openable zip and gets its own extension + double-click handler:</p> <pre><code>[0:5] \"SLIME\"  [5] version 0x01  [6] flags 0x00  [7] reserved\n[8:]  zip bytes, out[i] = zip[i] XOR key[i%32] XOR (i &amp; 0xFF)\n      key = SHA-256(\"PolyForge-Slime-v1\")\n</code></pre> <p>The identical transform lives in three places and is covered by a pinned test vector (<code>df5758d227b17001</code> for 8 zero bytes) so they can't drift:</p> <ul> <li>Go: <code>internal/kumi/slime.go</code> (the app reads packs)</li> <li>PowerShell: <code>scripts/slime-lib.ps1</code> (local packager)</li> <li>PHP: <code>website/api/slime-lib.php</code> (admin online packager)</li> </ul> <p>This is obfuscation, not encryption. The key is a constant, so anyone with the source can reverse it \u2014 its purpose is format obscurity and branding. Real access control for private packs is the server-side password gate (<code>api/pack-access.php</code>), never the container.</p> <p>On first launch the app registers <code>.polypack</code> with Windows (per-user registry, no admin/drivers) so double-clicking a pack opens it in PolyForge \u2014 see <code>internal/kumi/firstrun.go</code> + <code>fileassoc_windows.go</code>.</p>"},{"location":"modpack-format/#archive-layout-inside-the-container","title":"Archive layout (inside the container)","text":"<p>Unwrapped, a pack is a zip:</p> <pre><code>turtel-smp-1.0.0.polypack   (contains, once unwrapped:)\n\u251c\u2500\u2500 pack-manifest.json     identity + mod versions (drives updates)\n\u251c\u2500\u2500 launchers.json         per-launcher info fields (installer generates\n\u2502                          the actual launcher files from these)\n\u2514\u2500\u2500 overrides/             copied into the instance/.minecraft as-is\n    \u251c\u2500\u2500 mods/\n    \u251c\u2500\u2500 config/\n    \u251c\u2500\u2500 resourcepacks/\n    \u251c\u2500\u2500 shaderpacks/\n    \u2514\u2500\u2500 ...any other minecraft folders included at pack time\n</code></pre> <p>The packager also emits <code>&lt;id&gt;-&lt;version&gt;.manifest.json</code> (a standalone, un-obfuscated copy of <code>pack-manifest.json</code>) next to the <code>.polypack</code>, so the website can host just the manifest for update checks without clients downloading the full pack.</p>"},{"location":"modpack-format/#pack-manifestjson","title":"pack-manifest.json","text":"<pre><code>{\n  \"schemaVersion\": 1,\n  \"id\": \"turtel-smp\",\n  \"name\": \"Turtel SMP\",\n  \"version\": \"1.0.0\",\n  \"minecraft\": \"1.20.1\",\n  \"loader\": { \"type\": \"quilt\", \"version\": \"0.22.0\" },\n  \"created\": \"2026-07-04T00:00:00Z\",\n  \"mods\": [\n    {\n      \"file\": \"sodium-fabric-0.5.3.jar\",\n      \"id\": \"sodium\",\n      \"name\": \"Sodium\",\n      \"version\": \"0.5.3\",\n      \"sha256\": \"\u2026\",\n      \"sha1\": \"\u2026\",\n      \"source\": {\n        \"provider\": \"modrinth\",\n        \"projectId\": \"AANobbMI\",\n        \"versionId\": \"\u2026\",\n        \"url\": \"https://cdn.modrinth.com/data/\u2026/sodium-fabric-0.5.3.jar\"\n      }\n    }\n  ],\n  \"overrides\": {\n    \"folders\": [\"mods\", \"config\"],\n    \"fileCount\": 123,\n    \"totalBytes\": 456789,\n    \"files\": [\n      { \"path\": \"mods/sodium-fabric-0.5.3.jar\", \"sha256\": \"\u2026\", \"size\": 456789 },\n      { \"path\": \"config/sodium.properties\", \"sha256\": \"\u2026\", \"size\": 42 }\n    ]\n  }\n}\n</code></pre> <p>The <code>mods</code> array is the only thing used for update decisions. The installer compares the installed manifest against the hosted one and computes added / removed / version-changed mods (see <code>ComparePackMods</code> in <code>internal/kumi/packformat.go</code>). Entries are keyed by <code>id</code> \u2014 the authoritative mod id read from the loader metadata inside each jar \u2014 falling back to <code>name</code> for packs built before ids were emitted.</p>"},{"location":"modpack-format/#mod-identity-source-how-the-fields-are-filled","title":"Mod identity + source (how the fields are filled)","text":"<p>Both packagers read each mod's metadata from inside the archive rather than trusting the filename:</p> File inside the jar Loader Fields <code>fabric.mod.json</code> Fabric <code>id</code>, <code>version</code>, display <code>name</code> <code>quilt.mod.json</code> (<code>quilt_loader</code>) Quilt <code>id</code>, <code>version</code>, <code>metadata.name</code> <code>META-INF/mods.toml</code> / <code>META-INF/neoforge.mods.toml</code> Forge / NeoForge <code>modId</code>, <code>version</code> (<code>${file.jarVersion}</code> resolves via <code>MANIFEST.MF</code> <code>Implementation-Version</code>), <code>displayName</code> <code>litemod.json</code> LiteLoader (<code>.litemod</code> files, legacy) <code>name</code> (used as id), <code>version</code> <p>The filename split (<code>name-version.jar</code> at the last hyphen-digit) is only the fallback when no metadata is readable.</p> <p><code>sha1</code> keys a bulk Modrinth lookup (<code>POST /v2/version_files</code>): mods found on Modrinth get a <code>source</code> block (<code>provider</code>, <code>projectId</code>, <code>versionId</code>, direct <code>url</code>), which makes packs traceable to their upstream projects and lets a future update path re-fetch a mod from Modrinth instead of shipping the bytes. CurseForge has an equivalent (<code>POST /v1/fingerprints</code>, murmur2) but requires a partner <code>x-api-key</code> \u2014 planned once a key is provisioned. Mods without a match (private/renamed jars) simply have no <code>source</code>.</p>"},{"location":"modpack-format/#loader-version-resolution","title":"Loader version resolution","text":"<p>The PowerShell packager resolves and validates <code>loader.version</code> against each loader's official metadata (<code>-LoaderVersion latest</code> \u2014 or empty \u2014 picks the newest stable for <code>-McVersion</code>; an explicit version is validated with a warning):</p> Loader Source of truth Fabric <code>meta.fabricmc.net/v2/versions/loader/{mc}</code> (stable flag) Quilt <code>meta.quiltmc.org/v3/versions/loader/{mc}</code> Forge <code>files.minecraftforge.net/.../promotions_slim.json</code> (recommended \u2192 latest), validated against the Forge Maven NeoForge <code>maven.neoforged.net/releases/net/neoforged/neoforge/maven-metadata.xml</code> (MC <code>1.X.Y</code> \u2192 NeoForge <code>X.Y.*</code>; 1.20.1 is the legacy <code>net.neoforged:forge</code> artifact and must be passed explicitly) LiteLoader / vanilla recorded verbatim, never resolved <p><code>-McVersion</code> itself is sanity-checked against Mojang's <code>piston-meta.mojang.com/mc/game/version_manifest_v2.json</code>. Every network step degrades to a warning when unreachable, and <code>-Offline</code> skips them all \u2014 only <code>-LoaderVersion latest</code> hard-requires the network.</p>"},{"location":"modpack-format/#overridesfiles-per-file-integrity","title":"<code>overrides.files</code> \u2014 per-file integrity","text":"<p><code>overrides.files</code> lists every file the pack ships (path relative to <code>overrides/</code>, forward slashes) with its SHA-256 and size. Both packagers emit it; the installer re-hashes each extracted file against this list and refuses the install if anything is wrong (<code>VerifyInstalledPack</code> / <code>verifyFilesOnDisk</code> in <code>internal/kumi/packformat.go</code>). This catches corruption, tampering, and truncated/incomplete downloads before they cause problems in-game. Packs built before this field existed simply omit it and verification is skipped (backward compatible). This same per-file hash list is the foundation for file-level delta updates (below).</p>"},{"location":"modpack-format/#launchersjson-one-pack-every-launcher","title":"launchers.json \u2014 one pack, every launcher","text":"<p>The pack is launcher-agnostic: <code>launchers.json</code> carries info fields for every supported launcher, and the installer generates each launcher's real files (profiles, instance configs) from those fields + the manifest. The packager never targets a single launcher and never ships launcher-specific files, so the same <code>.polypack</code> installs everywhere.</p> <pre><code>{\n  \"schemaVersion\": 1,\n  \"defaults\": { \"minMemoryMb\": 2048, \"recommendedMemoryMb\": 4096, \"javaArgs\": \"\", \"iconPath\": \"\" },\n  \"launchers\": {\n    \"vanilla\":       { \"profileName\": \"...\", \"instanceName\": \"...\" },\n    \"multimc\":       { \"profileName\": \"...\", \"instanceName\": \"...\" },\n    \"prismlauncher\": { \"profileName\": \"...\", \"instanceName\": \"...\" },\n    \"modrinth\":      { \"profileName\": \"...\", \"instanceName\": \"...\" },\n    \"curseforge\":    { \"profileName\": \"...\", \"instanceName\": \"...\" }\n    /* ...all 21 supported launchers... */\n  }\n}\n</code></pre> <p>Generation is data-driven through the registry in <code>internal/kumi/packformat.go</code> (<code>launcherTargets</code>): each launcher declares where overrides go (<code>InstanceSubdirFor</code>) and a <code>Generate</code> writer (<code>internal/kumi/gen_launchers.go</code>). At install time the app derives the instance layout from the chosen path (<code>PlanInstallDirs</code>: launcher root \u2192 <code>instances/&lt;name&gt;/&lt;gamedir&gt;</code>, <code>profiles/&lt;name&gt;</code>, ...), extracts + verifies the overrides, then writes the launcher's own files. Every schema was captured from a real install on the reference machine (<code>TemporaryDetectRef/MachineTest_01/INSTANCES</code>):</p> Launcher Generated at install time vanilla <code>launcher_profiles.json</code> entry (gameDir, icon, <code>-Xmx</code> args); Fabric/Quilt version JSON fetched from the loader meta API into <code>versions\\</code> so the profile launches immediately (Forge/NeoForge: run their installer once) MultiMC / PolyMC / UltimMC legacy headerless <code>instance.cfg</code> + <code>mmc-pack.json</code> (components from <code>minecraft</code> + <code>loader</code>) Prism / ShatteredPrism / PineconeMC (elyprism) / Fjord / Freesm <code>[General]</code>-style <code>instance.cfg</code> + <code>mmc-pack.json</code> Modrinth App (Theseus) profile row registered directly in <code>app.db</code> (pure-Go SQLite; app-version-specific columns templated from an existing profile row) CurseForge <code>minecraftinstance.json</code> (guid, <code>baseModLoader</code>, memory; the app repairs the loader <code>versionJson</code> on first open) GDLauncher Carbon <code>instance.json</code> XMCL <code>instance.json</code> (runtime block) Dawn <code>profile.json</code> (schemaVersion 3) + <code>content-index.json</code> Polymerium <code>profile.json</code> (Trident purl-style loader id) ATLauncher / Technic / QWERTZ / BakaXL / SKLauncher / HMCL no generator (embedded-manifest format, uncaptured schema, or unsupported) \u2014 overrides still extract; the log says to add the instance manually <p>Generation never fails a verified install: every writer degrades to a log note (missing launcher state, unreachable meta API, ...) so worst case the files are in place and the user adds the instance in the launcher UI.</p>"},{"location":"modpack-format/#update-flow","title":"Update flow","text":"<ol> <li>App fetches the hosted <code>&lt;id&gt;-&lt;latest&gt;.manifest.json</code> (URL comes from the    remote content manifest / pack-access endpoint).</li> <li>Diffs the latest <code>overrides.files</code> against the locally installed manifest    copy (<code>.polyforge-pack.json</code>) by path + sha256:</li> <li>unchanged (same path + hash) \u2192 left on disk, never re-downloaded,</li> <li>changed / added \u2192 fetched and written,</li> <li>removed \u2192 deleted.    User files (not listed in the manifest) are never touched.</li> <li>Every fetched file is hash-verified against the manifest before it is    swapped into place \u2014 the same integrity guarantee as a fresh install.</li> </ol>"},{"location":"modpack-format/#delta-updates-bandwidth","title":"Delta updates (bandwidth)","text":"<p>Because step 2 already knows exactly which files changed, an update only needs to transfer the changed ones. Two hosting models:</p> <ul> <li>File-level (recommended): host each shipped file content-addressed by its   hash (<code>/packs/&lt;id&gt;/objects/&lt;sha256&gt;</code>). The client fetches only the objects it   doesn't already have; unchanged files (and files shared across versions or   packs) are free. Verification is automatic \u2014 the object is its hash.</li> <li>Whole-pack (today): host the full <code>&lt;id&gt;-&lt;version&gt;.polypack</code>. The diff   still avoids rewriting unchanged files on disk, but the whole pack is   downloaded. Simplest to host; no per-object endpoint.</li> </ul> <p>Byte-level binary diffs (bsdiff/xdelta) are intentionally not used: pack payloads are mostly jars that change wholesale between versions, so file-level delta captures nearly all the savings without a pure-Go patch codec.</p>"},{"location":"modpack-format/#what-ships-vs-what-never-ships","title":"What ships vs. what never ships","text":"<p>Based on analysis of a real Modrinth/Theseus profile (<code>Modrinth/profiles/&lt;name&gt;</code> is a full .minecraft-style instance dir):</p> Ships in overrides/ Never ships (user data / caches) <code>mods/</code> <code>saves/</code>, <code>screenshots/</code>, <code>logs/</code>, <code>crash-reports/</code> <code>config/</code>, <code>defaultconfigs/</code> <code>journeymap/</code> (map cache, thousands of files) <code>resourcepacks/</code>, <code>shaderpacks/</code> <code>essential/</code>, <code>emotes/</code> (mod caches) <code>datapacks/</code>, <code>scripts/</code>, <code>kubejs/</code> <code>.fabric/</code>, <code>debug/</code>, <code>downloads/</code>, <code>schematics/</code> root: <code>options.txt</code>, <code>servers.dat</code> root: <code>usercache.json</code>, <code>ops.json</code>, <code>whitelist.json</code>, <code>hotbar.nbt</code>, <code>hs_err_*.log</code>, <code>replay_*.log</code>, <code>command_history.txt</code> <p>Both packagers (the PowerShell script and the admin panel's online tool) apply this filter automatically.</p> <p>Mod filenames in the wild are too inconsistent for reliable parsing (<code>entity_texture_features_26.1-fabric-7.1.jar</code>, <code>Gamma-Utils-3.0.0+mc26.1.jar</code>), so both packagers read the loader metadata from inside each jar (see \"Mod identity + source\" above); the filename split is only the fallback.</p>"},{"location":"modpack-format/#open-items","title":"Open items","text":"<ul> <li>Generators for the remaining launchers: ATLauncher (its <code>instance.json</code>   embeds full Mojang + loader manifests, so it needs the meta APIs at install   time), QWERTZ (capture its <code>profiles.json</code> master-list schema from the test   machine), Technic / BakaXL / SKLauncher / HMCL (unsupported or untested).</li> <li>CurseForge fingerprint matching in the packagers (<code>POST /v1/fingerprints</code>,   murmur2) once a partner <code>x-api-key</code> is provisioned.</li> </ul>"}]}